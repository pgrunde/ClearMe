<h1>Click on a form to edit</h1><%= link_to "Add form", new_form_path %><br/>

<ul>
  <% @forms.each do |form| %>
    <li><%= link_to form.title, edit_form_path(form) %><%= link_to icon('close fa-1x'), form_path(form), class: "form-delete", method: :delete %></li>
  <% end %>
</ul>
